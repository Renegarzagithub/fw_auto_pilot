function "public"."get_all_widgets()" {
  text = """

	SELECT record_id,
           description 
	FROM widgets
"""
  returnType = SETOF public.widgets
  arguments = [
  ]
  language = sql
  owner = postgres
}

